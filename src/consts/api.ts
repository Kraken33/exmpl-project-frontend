const HTTP_CONTENT_TYPES = {
  json: "application/json",
};

enum HttpMethods {
  "POST" = "POST",
  "GET" = "GET",
  "PUT" = "PUT",
  "DELETE" = "DELETE",
}

export { HTTP_CONTENT_TYPES, HttpMethods };
