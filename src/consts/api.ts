const HTTP_CONTENT_TYPES = {
  json: "application/json"
};

enum HTTP_METHODS {
  "POST" = "POST",
  "GET" = "GET",
  "PUT" = "PUT",
  "DELETE" = "DELETE"
}

export { HTTP_CONTENT_TYPES, HTTP_METHODS };
